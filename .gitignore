# Dependencies
node_modules/

# Environment variables (keep secret)
backend/.env

# SQLite legacy files (no longer used — project migrated to Supabase)
backend/*.db
backend/*.db-wal
backend/*.db-shm

# Uploaded product images (stored as base64 in Supabase, not on disk)
backend/uploads/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# VS Code
.vscode/

.vercel
